#include <stdio.h>
/**
 * main: -for
 * Return: Returns 0
 */
int main(void)
{
	for (int i = 0; i <= 10; i++)
	{
	        {
		if (i==6)
		continue;
		}
	
	printf("number%d\n", i);
        }
	return (0);
}
